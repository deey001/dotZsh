# Custom aliases for productivity in Zsh
alias ll='ls -alF'  # List all files in long format with classification
alias la='ls -A'    # List all files except . and ..
alias l='ls -CF'    # List files with classification
alias grep='grep --color=auto'  # Colorized grep output